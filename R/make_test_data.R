# fct_maker <- function(size, missing = FALSE) {
#   x <- sample(state.name, size = size, replace = TRUE)
#   y <- factor(x = x, levels = unique(sort(x)))
#   if (isTRUE(missing)) {
#     sample(x = z, size = size, replace = TRUE)
#   } else {
#     sample(x = z, size = size, replace = TRUE)
#   }
# }
# ord_maker <- function(size, missing = FALSE) {
#   if (isTRUE(missing)) {
#     sample(x = , size = size, replace = TRUE)
#   } else {
#     sample(x = , size = size, replace = TRUE)
#   }
# }
